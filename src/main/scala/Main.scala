object Main {
  def main(args: Array[String]): Unit = {
    //sc.parallelize([3,4,5]).flatMap(lambda x: [x, x*x]).collect()
    println("Hello world!")
  }

  def fileLoad ():
  Unit = {
  }
}